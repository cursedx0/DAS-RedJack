<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Entrega1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema Claro Personalizado -->
    <style name="Theme.App.LightRed" parent="Theme.Material3.Light">
        <item name="colorPrimary">#fc2511</item>
        <item name="colorPrimaryVariant">#d61908</item>
    </style>

    <style name="Theme.App.DarkRed" parent="Theme.Material3.Dark">
        <item name="colorPrimary">#fc2511</item>
        <item name="colorPrimaryVariant">#d61908</item>
        <item name="colorOnPrimary">#ffffff</item>
    </style>

    <style name="Theme.Entrega1" parent="Base.Theme.Entrega1" />

    <style name="Theme.Entrega1.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
        <item name="appWidgetRadius">16dp</item>
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
        <item name="appWidgetInnerRadius">8dp</item>
    </style>

    <style name="Theme.Entrega1.AppWidgetContainer" parent="Theme.Entrega1.AppWidgetContainerParent">
        <!-- Apply padding to avoid the content of the widget colliding with the rounded corners -->
        <item name="appWidgetPadding">16dp</item>
    </style>
</resources>